# Vim
alias vim="nvim"

# Cat with color
alias cat="ccat"

# Docker ps
alias ds="docker ps --format 'table {{.ID}}\\t{{.Names}}\t{{.Status}}'"

# Please to re-run with sudo
alias please='sudo $(fc -ln -1)'

# NTNU VPN script
alias vpn="cd ~/Desktop/vpn && ./vpn.sh"
